# UniFi CLI (неофициальный)

Инструмент командной строки для управления контроллером UniFi. Позволяет авторизовывать клиентов, управлять устройствами, перезагружать точки доступа и выполнять другие операции через API UniFi.

## 📦 Установка

### Требования

Установите необходимые зависимости:

```sh
apt-get update
apt-get install curl jq realpath

# Загрузите UniFi CLI
git clone https://github.com/xezpeleta/unifi-cli.git

# Опционально: создайте символическую ссылку в /usr/local/bin
ln -s <путь-к-загрузке>/unifi /usr/local/bin/unifi
```

### Необходимые пакеты

- **curl** - для выполнения HTTP запросов к API контроллера
- **jq** - для обработки JSON ответов
- **realpath** - для определения путей к файлам

## ⚙️ Конфигурация

Переименуйте файл `unifi.conf.dist` в `unifi.conf` и измените следующие параметры:

```bash
# Обязательные переменные

username=admin                      # Имя пользователя контроллера
password=unf                        # Пароль от контроллера
baseurl=https://unificontroller:8443  # URL контроллера UniFi
site=default                        # Название сайта (обычно default)
```

### Пример настройки

```bash
# Скопируйте шаблон конфигурации
cp unifi.conf.dist unifi.conf

# Отредактируйте конфигурацию
nano unifi.conf
```

## 🚀 Использование

### Основной CLI инструмент

```sh
./unifi [опции] [команда]
```

### Доступные команды

#### Просмотр устройств

```bash
# Список всех устройств
./unifi --list all

# Список только точек доступа
./unifi --list ap

# Показать информацию о конкретном устройстве (по MAC-адресу)
./unifi --show=44:d9:e1:99:99:99
```

#### Управление устройствами

```bash
# Перезагрузить устройство по MAC-адресу
./unifi --reboot=44:d9:e1:99:99:99

# Обновить устройство по MAC-адресу
./unifi --upgrade=44:d9:e1:99:99:99

# Постепенная перезагрузка всех точек доступа (rolling reboot)
./unifi -R --reboot

# Постепенное обновление всех точек доступа (rolling upgrade)
./unifi -R --upgrade
```

#### Авторизация клиентов

Для авторизации клиентов используйте скрипт `authorize-client.sh`:

```bash
# Авторизовать клиента на 1 час
./authorize-client.sh 3c:84:6a:12:45:8f 60

# Авторизовать клиента на 24 часа с ограничениями скорости
./authorize-client.sh 3c:84:6a:12:45:8f 1440 up=2000 down=5000
```

### Опции

- `-R` - включить режим постепенного выполнения (rolling upgrade/reboot)
- `-d` или `--debug` - включить режим отладки с детальным логированием
- `-h` или `--help` - показать справку
- `-v` или `--version` - показать версию

### Примеры

```bash
# Список точек доступа
./unifi --list ap

# Перезагрузить устройство
./unifi --reboot 44:d9:e1:99:99:99

# Постепенное обновление с отладкой
./unifi -d -R --upgrade
```

## 📚 Документация

- [Quick Start (RU)](QUICK_START_RU.md) - 🚀 Быстрый старт и основные команды
- [Authorization Guide](AUTHORIZATION_GUIDE.md) - 🔑 Руководство по авторизации клиентов на контроллере UniFi
- [Logging README](doc/README_LOGGING.md) - Быстрый старт с системой логирования
- [Logging Documentation](doc/LOGGING.md) - Подробная документация по системе логирования
- [Logging Summary](doc/LOGGING_SUMMARY.md) - Краткое описание системы логирования
- [Changelog - Logging](doc/CHANGELOG_LOGGING.md) - История изменений системы логирования

## 🔑 Авторизация клиентов

### Скрипт authorize-client.sh

Специальный скрипт для авторизации гостевых клиентов на контроллере UniFi:

```bash
./authorize-client.sh <MAC-адрес> <минуты> [параметры]
```

### Параметры авторизации

- `up=N` - ограничение скорости загрузки в kbps
- `down=N` - ограничение скорости скачивания в kbps
- `bytes=N` - ограничение трафика в мегабайтах

### Примеры авторизации

```bash
# На 1 час без ограничений
./authorize-client.sh 3c:84:6a:12:45:8f 60

# На 24 часа
./authorize-client.sh 3c:84:6a:12:45:8f 1440

# На неделю
./authorize-client.sh 3c:84:6a:12:45:8f 10080

# С ограничениями скорости
./authorize-client.sh 3c:84:6a:12:45:8f 120 up=2000 down=5000

# С ограничением трафика
./authorize-client.sh 3c:84:6a:12:45:8f 60 bytes=100
```

Подробнее см. [Authorization Guide](AUTHORIZATION_GUIDE.md)

## 🔧 Дополнительные возможности

### Создание собственных скриптов

Используйте файл `my-unifi-script.sh` как шаблон для создания собственных скриптов:

```bash
#!/bin/bash

# Загрузка конфигурации
. unifi.conf

# Загрузка API библиотек
. unifi_sh_api
. unifi_sh_api_nonofficial

# Авторизация
unifi_login

# Ваши команды здесь
# Например:
# unifi_list_sta | python -m json.tool
# unifi_authorize_guest aa:bb:cc:dd:ee:ff 60

# Выход
unifi_logout
```

### Доступные API функции

#### Управление клиентами
- `unifi_list_sta` - список подключенных клиентов
- `unifi_authorize_guest <mac> <minutes> [параметры]` - авторизовать гостя
- `unifi_unauthorize_guest <mac>` - отменить авторизацию
- `unifi_block_sta <mac>` - заблокировать клиента
- `unifi_reconnect_sta <mac>` - переподключить клиента

#### Управление устройствами
- `unifi_list_dev` - список всех устройств
- `unifi_list_ap` - список точек доступа
- `unifi_reboot_dev <mac>` - перезагрузить устройство
- `unifi_reboot_all_ap` - перезагрузить все точки доступа
- `unifi_upgrade_dev <mac>` - обновить устройство
- `unifi_ap_rolling_upgrade` - постепенное обновление
- `unifi_ap_rolling_upgrade_cancel` - отменить постепенное обновление

#### Резервное копирование
- `unifi_backup [filename]` - создать резервную копию контроллера

#### Ваучеры
- `unifi_create_voucher <minutes> <n> [параметры]` - создать ваучеры
- `unifi_get_vouchers [token]` - получить информацию о ваучерах
- `unifi_delete_voucher <id>` - удалить ваучер

## 📊 Логирование

Проект включает продвинутую систему логирования с поддержкой:
- Уровней логирования (INFO, DEBUG, ERROR)
- Временных меток
- Отдельного логирования для API, скриптов и основного CLI
- Детального отслеживания HTTP запросов и ответов

Включите режим отладки с помощью флага `-d`:

```bash
./unifi -d --list all
./authorize-client.sh 3c:84:6a:12:45:8f 60
```

## 🛠️ Устранение неполадок

### Проблема: Ошибка "Config file not found"

**Решение:**
```bash
# Убедитесь, что файл конфигурации существует
ls -la unifi.conf

# Если нет, создайте его из шаблона
cp unifi.conf.dist unifi.conf
nano unifi.conf
```

### Проблема: Ошибка авторизации (Login error)

**Возможные причины:**
1. Неверный логин или пароль в `unifi.conf`
2. Контроллер недоступен
3. Неправильный URL контроллера

**Решение:**
```bash
# Проверьте доступность контроллера
ping <IP-адрес>

# Проверьте конфигурацию
cat unifi.conf

# Включите режим отладки
./unifi -d --list all
```

### Проблема: Клиент не получает доступ после авторизации

**Решение:**
1. Проверьте правильность MAC-адреса
2. Клиент должен переподключиться к Wi-Fi сети
3. Убедитесь, что гостевой портал настроен на контроллере
4. Проверьте наличие ограничений на контроллере

## 📝 Примечания

- Все команды выполняются через HTTPS (поддержка TLSv1)
- Сертификаты SSL не проверяются (флаг `--insecure`)
- MAC-адреса можно указывать с двоеточиями или дефисами
- Файл cookie сохраняется в `/tmp/unifi_cookie`

## 🔗 Ресурсы

Проект использует [официальную библиотеку UniFi API](https://dl.ubnt.com/unifi/5.4.11-6cbeae59e7/unifi_sh_api) с дополнительными неофициальными расширениями.

## 📄 Лицензия

См. файл [LICENSE](LICENSE)

## 🤝 Вклад в проект

Оригинальный проект: [xezpeleta/unifi-cli](https://github.com/xezpeleta/unifi-cli)

## 📞 Поддержка

При возникновении проблем:
1. Проверьте документацию в папке `doc/`
2. Включите режим отладки (`-d`)
3. Убедитесь, что контроллер доступен
4. Проверьте правильность настроек в `unifi.conf`

---

**Версия:** 0.1.3_alpha  
**Язык:** Bash  
**Платформа:** Linux
